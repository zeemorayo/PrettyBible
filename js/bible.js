document.addEventListener("DOMContentLoaded", function () {
    console.log("Bible app is running!");

    // Example: Load a verse dynamically (we'll expand on this later)
    const bibleContainer = document.getElementById("bible-container");
    bibleContainer.innerHTML = "<h2>Genesis 1:1</h2><p>In the beginning, God created the heavens and the earth.</p>";

    // Placeholder for features (we’ll add search, highlight, and more later)
});
